{
  "name": "jenkins-cicd-demo",
  "version": "1.0.0",
  "description": "Simple Node.js app for demonstrating a full CI/CD pipeline with Jenkins",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --ci"
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": ["lcov", "text"]
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
